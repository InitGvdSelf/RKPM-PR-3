import 'package:flutter/material.dart';

class DocsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    final text = '''
Назначение
Приложение демонстрирует основные экраны телефонного клиента: профиль, настройки, чаты и встроенную справку.

Навигация
Главный экран содержит кнопки для перехода в разделы. Возврат осуществляется стрелкой назад в AppBar.

Профиль
Заполните имя и статус, при необходимости обновите аватар. Нажмите «Сохранить», чтобы зафиксировать введённые данные.

Настройки
Переключите тёмную тему, уведомления и звуки. Изменения применяются мгновенно в рамках экрана.

Чаты
Выберите чат из списка слева или создайте новый. Введите сообщение и отправьте его. Сообщения сохраняются в текущем сеансе.

Ограничения
Данные не сохраняются между перезапусками. Демонстрационная логика предназначена для учебных целей.
''';
    return Scaffold(
      appBar: AppBar(title: Text('Документация')),
      body: SingleChildScrollView(
        padding: EdgeInsets.all(16),
        child: Text(text, style: TextStyle(fontSize: 16)),
      ),
    );
  }
}